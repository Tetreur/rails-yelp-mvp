<h1>Créer un restaurant</h1>

<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category, collection: Restaurant::CATEGORY, prompt: "Choisir une catégorie" %>
  <%= f.button :submit %>
<% end %>

<p><%= link_to "back", restaurants_path %></p>
